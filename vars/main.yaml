---

nvm_install_dir:  '/opt/nvm'
node_path:        '/usr/local/node'
nvm_path:         '/usr/local/nvm'
nvm_group:        'nvm'

global_pkgs:
  all:
    - pm2
    - pm2-logrotate
    - pmx
  gui:
    - bower

executables:
  all:
    - node
    - npm
    - pm2
    - pm2-dev
    - pm2-docker
    - rundev
  gui:
    - bower
