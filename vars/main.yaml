---

nvm_install_dir:  '/opt/nvm'
node_path:        '/usr/local/node'
nvm_path:         '/usr/local/nvm'
nvm_group:        'nvm'

pm2:
  path:               "/home/{{ ansible_user }}/.pm2"
  pids:               "/home/{{ ansible_user }}/.pm2/pids"
  logs:               "/home/{{ ansible_user }}/.pm2/logs"
  startup_type:       "systemd"

globals:
  all:
    execs:
      - node
      - npm
      - pm2
      - pm2-dev
      - pm2-docker
      - rundev
      - yarn
      - yarnpkg
    npm_pkgs:
      - pm2
      - pmx
      - yarn
    pm2_mods:
      - pm2-logrotate
  gui:
    execs:
      - bower
    npm_pkgs:
      - bower
    pm2_mods: []
